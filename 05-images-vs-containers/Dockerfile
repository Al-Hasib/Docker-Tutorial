# Minimal on purpose — the point of this video is the image/container
# relationship, not Dockerfile syntax (that's topic 7).
FROM python:3.12-slim
WORKDIR /app
COPY whoami.py .
CMD ["python", "whoami.py"]
